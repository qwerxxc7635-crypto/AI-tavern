# Windows v0.1 收口与验收清单

更新日期：2026-08-01

策略：Windows-first；M9及M10-T04保持`DEFERRED`，不属于本阶段完成条件。

本文件不替代`docs/spec.md`或`docs/TASKS.md`。它把现有验收要求映射为Windows首版的发布证据、已知缺口和执行顺序；勾选只允许基于当前构建、测试、运行或产物证据。

## 当前基线

- [x] `M8-T04`已由提交`5904a87`完成，提交后工作树干净。
- [x] Windows前端生产构建和Tauri Release `--no-bundle`已通过；可执行文件不依赖Vite开发服务器。
- [x] Fake Provider纵向用例、真实SQLite持久化、模型合同、恢复、备份和存档迁移已有自动测试。
- [x] `WV0.1-T01`已启用当前用户NSIS安装包，并完成隔离安装目录的安装、启动和卸载验证。
- [x] `M10-T01`已完成状态机、D20、关系/时钟和AI状态补丁的88项发布行为矩阵。
- [x] `M10-T02`已让DeepSeek、Qwen、OpenRouter、Ollama和自定义兼容服务通过同一Provider合同矩阵。
- [x] `M10-T03`已用同一临时SQLite存档自动串联完整纵向切片、两次重启、模型切换及存档导出/删除/重导入。
- [x] 已完成覆盖完整Windows纵向流程、关闭重启、模型配置/故障恢复及存档往返的发布候选烟测，记录见`docs/WINDOWS_ACCEPTANCE_0.1.md`。
- [x] 已完成面向玩家的Windows隐私、数据位置、日志/诊断、卸载数据策略、发布说明和已知限制。

## 1. 安装与启动

- [x] 生成Windows NSIS安装包，版本、产品名、标识和应用图标正确。
- [x] 在非开发目录安装并启动，运行时不依赖开发服务器、仓库`node_modules`或手工命令。
- [x] 首次启动使用系统应用数据目录的生产数据库，不读取仓库测试数据库；既有首次启动烟测与真实SQLite测试覆盖空库迁移。
- [x] 新用户看到存档空状态和创建入口；无需接触数据库或命令行。
- [x] `docs/WINDOWS_INSTALL.md`记录安装、WebView2、数据位置和卸载保留策略；隔离安装验证卸载程序与注册表清理，真实应用数据保持完整。

## 2. 核心游戏流程

- [x] 用发布候选完成：创建世界、锁定/重生成、创建角色、进入酒馆。
- [x] 与NPC连续对话、查看传闻、接受一个主任务。
- [x] 完成8至12回合冒险，覆盖本地D20检定与无检定回合。
- [x] 完成冒险结算，验证NPC、奖励、酒馆、世界时钟和档案变化后返回酒馆。
- [x] 关闭并重启应用，继续同一存档且已提交状态不丢失、不重复。

## 3. 模型配置

- [x] Fake Provider可用于离线自动验收。
- [x] OpenAI-Compatible、DeepSeek、Qwen、OpenRouter、Ollama和自定义兼容服务具有配置/合同测试。
- [x] API Key由Windows Credential Manager保存，SQLite只保存不透明`credentialRef`。
- [x] 模型能力登记、结构化输出模式降级、路由与连接错误分类已有自动测试。
- [x] 在发布候选设置页人工验证新增、测试、设为默认/备用、切换模型、删除凭据及重启后的显示行为。
- [x] 首版不要求产生费用的真实Provider调用；隐私与发布说明已明确当前Fake生成、云连接测试和本地合同测试边界。

## 4. 稳定性与恢复

- [x] 标准错误分类、同模型重试、备用模型和跨厂商确认有自动测试。
- [x] 结构化输出一次修复、原文审计和最终失败不提交游戏状态有自动测试。
- [x] 完整回合AUTO快照、最近10个轮换、数据库完整备份和最近3份轮换有自动测试。
- [x] 中断请求、崩溃回合、取消回退和长上下文压缩有自动测试。
- [x] 发布候选人工制造可恢复失败，确认恢复入口可见、操作文案清楚且不会重复提交回合。
- [x] 实际终止进程后重启，确认最近完整状态可恢复且主存档未损坏。

## 5. 存档能力

- [x] `.emtavern` v1格式、SHA-256、Schema和秘密边界已定义并测试。
- [x] 导出、导入、CREATE/OVERWRITE、覆盖前完整备份和IMPORT快照已测试。
- [x] Windows系统文件选择、保存位置选择和单文件拖放导入已实现并测试。
- [x] API Key/设备凭据排除、损坏包拒绝、失败不改变原存档已测试。
- [x] 在发布候选中实际执行系统对话框导出、备份保护的永久删除、重新导入并继续游戏。

## 6. Windows用户体验

- [x] 安装包、图标、0.1.0版本信息与卸载条目完整。
- [x] 首次启动引导或最小可理解空状态通过人工验收。
- [x] 关键生成、导入导出、恢复流程具有加载、成功和可行动错误反馈。
- [x] 在最低窗口860×600及常用1180×760、1366×768、1920×1080下无不可达的关键内容；小窗口通过内容区滚动访问底部操作。
- [x] 键盘焦点、Tab顺序、Enter/Escape与鼠标基本操作可完成关键流程。
- [x] 玩家文档说明无独立文本日志、SQLite审计/用户数据位置、凭据位置和卸载后数据策略。
- [x] 崩溃或中断后恢复入口可被普通用户发现。
- [x] 不以次要动画、高级视觉效果或非核心定制阻塞首版；这些归为P2。

## 7. 发布前质量门

- [x] Prettier、ESLint、TypeScript、全部Vitest和Node SQLite全绿。
- [x] Cargo fmt、严格Clippy与全部Rust测试全绿。
- [x] Windows前端生产构建全绿。
- [x] Tauri Release安装包构建全绿。
- [x] 实际Windows纵向烟测通过并记录环境、步骤和结果。
- [x] 对源码、配置、归档样本和产物执行秘密扫描。
- [x] 发布说明和已知限制完成。
- [x] 最终提交后工作树干净。

## 剩余任务分类

| 分类 | 任务 | Windows v0.1处理 |
| --- | --- | --- |
| P0 | `WV0.1-T01 Windows安装包与启动验收`（已完成） | 当前用户NSIS产物、非开发目录启动、生产数据路径与卸载保留策略已验证。 |
| P0 | `M10-T01 完成Domain单元测试`（已完成） | 状态机、骰子、关系、时钟和补丁验证的正常、边界、非法与不可变矩阵已通过。 |
| P0 | `M10-T02 完成Provider Contract Test`（已完成） | 5个正式启用Provider已通过统一的发现、连接、文本、JSON、错误与非法响应合同集合。 |
| P0 | `M10-T03 完成Windows端到端测试`（已完成） | 同一临时SQLite存档已自动完成14步纵向切片、两次重启、模型切换和存档往返。 |
| P0 | `M10-T06 Windows v0.1最终验收`（已完成） | Windows范围最终验收通过；延期iOS未计入结论。 |
| P1 | `M10-T05 完成隐私和数据说明`（Windows范围已完成） | Windows本地存储、当前联网、未来云上下文/跨厂商确认、密钥、归档和删除边界已说明；iOS章节后补。 |
| P1 | Windows UI与发布说明收尾 | 首次启动、空状态、错误入口、分辨率、键鼠、数据/日志位置和已知限制。 |
| P2 | 次要视觉与便利功能 | 次要动画、高级视觉效果、非核心Provider扩展和高级定制不阻塞首版。 |
| DEFERRED | `M9-T01`至`M9-T09`、`M10-T04` | Windows v0.1完成且具备macOS/Xcode环境后恢复。 |

## 新执行顺序

1. `WV0.1-T01 Windows安装包与启动验收`（已完成）。
2. `M10-T01 完成Domain单元测试`（已完成）。
3. `M10-T02 完成Provider Contract Test`（已完成）。
4. `M10-T03 完成Windows端到端测试`（已完成）；发布候选UI/恢复人工检查继续保留在最终清单。
5. `M10-T05 完成Windows隐私和数据说明`及发布说明（已完成）。
6. `M10-T06 Windows v0.1最终验收`（已完成），全质量门、秘密扫描和干净工作树检查通过。

## iOS恢复门

只有同时满足以下条件才恢复M9：Windows v0.1已按本清单完成；存在可用macOS/Xcode环境；移动端范围、真机和发布要求已经明确。恢复时继续使用原M9编号和顺序，不重写共享核心，也不把Windows专属API移入共享层。
