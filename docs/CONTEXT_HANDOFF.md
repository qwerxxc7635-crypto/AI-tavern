# Ember Tavern 上下文交接

## 当前状态

- 分支：`main`
- 最近提交：`M1-T07` 完成提交（以 `git log -1` 为准）
- 已完成里程碑：M0；M1 已完成 `M1-T01` 至 `M1-T07`
- 当前任务：`M1-T07` 已验收，准备提交
- 下一任务：`M1-T08 实现 D20 规则引擎`

## 架构摘要

- SQLite 是游戏事实唯一来源；AI输出必须经结构和业务规则验证。
- Campaign、世界事实、玩家、NPC、任务/物品和冒险协议已建立。
- AI文本与程序规则字段物理分离；冒险状态机拒绝非法迁移。
- 玩家属性和NPC关系由程序限制；NPC知识按角色隔离。

## 关键决策

- `DEC-001` 至 `DEC-006` 已记录 NPC数量、Cargo验收、基础类型、Campaign恢复、世界事实和AI/规则边界。

## 环境与限制

- Node.js `v24.17.0`，pnpm `11.9.0`；Rustc/Cargo `1.97.1`。
- 缓存、下载、构建和临时文件位于 `.local/`。
- iOS真机/签名需要 macOS/Xcode；真实云测试需要安全提供的 API Key。

## 最近成功验证

- `pnpm check`：通过。
- Vitest：7 个文件、58 个测试通过。
- Rust fmt、严格Clippy、workspace test通过。

## 恢复步骤

1. 完整读取规则、规格、任务、日志、决策、README、`LOG.md` 和本文件。
2. 检查 Git并设置 `.local/` 环境变量。
3. 从 `M1-T08` 在 domain 包实现可注入随机源的D20引擎、属性/装备/状态修正和四档难度。
4. 使用固定随机数测试边界，通过根质量门后独立提交。
