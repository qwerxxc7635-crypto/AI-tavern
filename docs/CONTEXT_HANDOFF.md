# Ember Tavern 上下文交接

## 当前状态

- 分支：`main`
- 最近提交：`M1-T08` 完成提交（以 `git log -1` 为准）
- 已完成里程碑：M0；M1 已完成 `M1-T01` 至 `M1-T08`
- 当前任务：`M1-T08` 已验收，准备提交
- 下一任务：`M1-T09 实现关系与世界时钟规则`

## 架构摘要

- SQLite是游戏事实唯一来源；AI输出必须经结构和业务规则验证。
- Campaign、世界、角色、NPC、任务/物品和冒险协议已建立。
- AI文本与程序规则物理分离；D20由可注入本地随机源结算。
- 玩家属性和NPC关系有程序边界；NPC知识按角色隔离。

## 关键决策

- `DEC-001`至`DEC-007`记录NPC数量、Cargo验收、基础类型、恢复、世界事实、AI/规则边界和随机源。

## 环境与限制

- Node.js `v24.17.0`，pnpm `11.9.0`；Rustc/Cargo `1.97.1`。
- 缓存、下载、构建和临时文件位于`.local/`。
- iOS真机/签名需要macOS/Xcode；真实云测试需要安全提供的API Key。

## 最近成功验证

- `pnpm check`：通过。
- Vitest：8个文件、71个测试通过。
- Rust fmt、严格Clippy、workspace test通过。

## 恢复步骤

1. 完整读取规则、规格、任务、日志、决策、README、`LOG.md`和本文件。
2. 检查Git并设置`.local/`环境变量。
3. 从`M1-T09`实现单回合关系变化上限、世界时钟范围/阶段触发和非法补丁拒绝。
4. 为越界不写入和时钟触发添加测试，通过根质量门后独立提交。
