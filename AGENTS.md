# Ember Tavern 开发规则

本文件中的规则适用于整个仓库，并在后续任务中持续生效。

## 需求与任务边界

- 严格遵循 `docs/spec.md`。
- 严格按照 `docs/TASKS.md` 的任务顺序开发。
- 每次只执行用户明确指定的任务。
- 不自动开始下一项任务。
- 不擅自扩大功能范围。
- 不提前实现未来阶段功能。
- 不提前接入真实大模型 API。
- 不提前全面开发 iOS 端。
- Windows 端纵向切片优先。
- 不为尚未存在的需求过度设计。

## 架构与数据安全

- 本地 SQLite 是游戏状态的唯一真实数据源。
- 云模型或本地模型都只能作为可替换的内容生成器。
- AI 输出必须经过结构和业务规则验证，不能直接修改游戏状态。
- API Key 不能写入代码、普通配置文件、日志或存档。
- 优先使用简单、清晰、可测试和可替换的实现。

## 质量与验收

- 禁止通过空实现、伪实现、写死结果或跳过验证完成任务。
- 禁止删除测试、降低校验标准或忽略错误来使检查通过。
- 重大架构决定必须记录在 `docs/DECISIONS.md`。
- 每次任务完成后必须更新 `docs/DEVELOPMENT_LOG.md`。
