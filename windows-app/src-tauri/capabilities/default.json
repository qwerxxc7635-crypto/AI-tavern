{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Ember Tavern main window.",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-version",
    "core:event:default",
    "dialog:allow-message",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
